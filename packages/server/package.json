{
  "name": "@truck/server",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "package": "node scripts/package.mjs",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@truck/core": "workspace:*"
  },
  "devDependencies": {
    "@truck/web": "workspace:*",
    "@types/node": "^24.10.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
